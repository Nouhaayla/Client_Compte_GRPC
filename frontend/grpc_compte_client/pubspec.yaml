name: grpc_compte_client
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  grpc:
  protobuf: 
  http: # For testing if needed

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: 
  protoc_plugin: # Generates Dart classes from proto

flutter:
  uses-material-design: true
